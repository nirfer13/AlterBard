# Bot dependencies. Versions match pyproject.toml / poetry.lock.
# Install with: pip install -r requirements.txt
#
# Playback is handled by the Lavalink server (Lavalink_Actual.jar + application.yml),
# not by Python itself - which is why there is no youtube-dl and no ffmpeg here.
# The bot only asks Lavalink, through wavelink, to search and stream from YouTube.

discord.py==2.3.2
wavelink==3.2.0
python-dotenv==1.0.0
yarl==1.9.2
